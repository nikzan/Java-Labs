# Полный план защиты лабораторной работы №4 (Generics & Functional Interfaces)

Этот документ содержит подробный разбор всего кода лабораторной работы (за исключением `Main` и `TestRunner`), структурированный для защиты перед преподавателем.

---

## Текст Задания (Справочно)

### Задание 1. Обобщенные типы. Типовые переменные, <>.

**Задание 1.1 - Обобщенная коробка.**
Создайте сущность Коробка, которая обладает следующими характеристиками:
*   Может хранить один произвольный объект в один момент времени.
*   Объект можно получить и разместить на хранение в любой момент времени.
*   Если объект забирают из коробки – ссылку на этот объект необходимо обнулить.
*   Если объект кладут в коробку, но она не пуста – необходимо выкинуть исключение.
*   Имеет метод проверки на заполненность.
*   Методы класса должны работать с тем типом данных, который был указан во время создания объекта.

Создайте Коробку которая может хранить целочисленное значение, разместите туда число 3.
Передайте Коробку в какой-либо метод, извлеките значение, и выведите его на экран.

**Задание 1.3 - Сравнимое.**
Создайте ссылочный тип Сравнимое, гарантирующий наличие по данной ссылке метода со следующими характеристиками:
*   Называется “сравнить”
*   Принимает объект.
*   Тип принимаемого объекта может быть изменен без изменения самого Сравнимого.
*   Возвращает целое число.

### Задание 2. Параметризации. Маски типов и их ограничения. Поиск максимума.
Создайте метод, принимающий набор Коробок из задачи 1.1 и возвращающий максимальное из их значений в формате double. Принимаемые методом Коробки могут быть параметризованы любыми видами чисел.

### Задание 3. Обобщенные методы. Автовывод типа.

**3.1 Функция.**
Разработайте такой метод, который будет принимать список значений типа T, и объект имеющий единственный метод apply. Данный метод надо применить к каждому элементу списка, и вернуть новый список значений типа P, при этом типы T и P могут совпадать, а могут не совпадать.
Используйте разработанный метод следующим образом:
*   Передайте в метод список со значениями:“qwerty”, “asdfg”, “zx”, а получите список чисел, где каждое число соответствует длине каждой строки.
*   Передайте в метод список со значениями: 1,-3,7, а получите список в котором все отрицательные числа стали положительными, а положительные остались без изменений
*   Передайте в метод список состоящий из массивов целых чисел, а получите список в котором будут только максимальные значения каждого из исходных массивов

**3.2 Фильтр.**
Разработайте такой метод, который будет принимать список значений типа T и объект имеющий единственный метод test (принимает T и возвращает boolean). Верните новый список типа T, из которого удалены все значения не прошедшие проверку условием.
Используйте разработанный метод следующим образом:
*   Передайте в метод список со значениями: “qwerty”, “asdfg”, “zx”, и отфильтруйте все строки имеющие менее трех символов
*   Передайте в метод список со значениями: 1,-3,7, и отфильтруйте все положительные элементы
*   Передайте в метод список состоящий из массивов целых чисел, а получите список в котором будут только те массивы, в которых нет ни одного положительного элемента

**3.3 Сокращение.**
Разработайте такой метод, который будет принимать список значений типа T и способ с помощью которого список значений можно свести к одному значению типа T, которое и возвращается из метода.
Используйте разработанный метод следующим образом:
*   Передайте в метод список со значениями: “qwerty”, “asdfg”, “zx”, и сформируйте одну большую строку, которая состоит из всех строк исходного списка.
*   Передайте в метод список со значениями: 1,-3,7, и верните сумму всех значений исходного списка.
*   Имеется список, состоящий из списков целых чисел, получите общеe количество элементов во всех списках. Подсказка: решить задачу можно в одно действие или последовательно использовать методы из 3.3.1 и 3.3.3.

Далее необходимо изменить разработанный метод таким образом, чтобы данный метод гарантированно не возвращал null и не выбрасывал ошибок в том случае, если исходный список пуст.

**3.4 Коллекционирование.**
Разработайте такой метод, который будет возвращать коллекцию типа P со значениями типа T.
Данный метод будет принимать:
*   Список исходных значений
*   Способ создания результирующей коллекции
*   Способ передачи значений исходного списка в результирующую коллекцию.

Используйте разработанный метод следующим образом:
*   Передайте в метод список со значениями: 1,-3,7, и верните их разбитыми на два подсписка, в одном из которых будут только положительные числа, а в другом только отрицательные.
*   Передайте в метод список со значениями: “qwerty”, “asdfg”, “zx”, “qw” и верните их разбитыми на подсписки таким образом, чтобы в любом подсписке были строки только одинаковой длины
*   Передайте в метод список со значениями: “qwerty”, “asdfg”, “qwerty”, “qw” и верните набор такого вида, который не может содержать одинаковые объекты.

---

## Введение
**Цель работы:** Научиться создавать универсальные классы и методы с использованием дженериков (Generics), понимать принципы работы с Wildcards (`?`), а также применять функциональные интерфейсы для обработки коллекций в стиле Stream API.

---

## Блок 1: Обобщенная Коробка (Box)
Файл: `ru.nikzan.generics.Box.java`

### 1.1 Объявление класса и хранение данных
```java
public class Box<T> {
    private T item;
    // ...
}
```
🗣 **Что говорить:**
> "Класс `Box` является обобщенным (generic). Параметр типа `T` позволяет создавать коробки для хранения объектов любого типа, например `Box<Integer>` или `Box<String>`. Поле `item` хранит содержимое коробки. Изначально оно равно `null`, что означает, что коробка пуста."

🎓 **Теория:**
*   **Параметрический полиморфизм:** Возможность писать код, который работает с данными разных типов одинаково.
*   **Type Erasure (Стирание типов):** В скомпилированном байт-коде `T` заменяется на `Object`. Проверки типов происходят только на этапе компиляции.

### 1.2 Метод put (Положить)
```java
public void put(T item) {
    if (item == null) {
        throw new IllegalArgumentException("Нельзя помещать null в коробку");
    }
    if (this.item != null) {
        throw new IllegalStateException("Коробка уже заполнена!");
    }
    this.item = item;
}
```
🗣 **Что говорить:**
> "Метод `put` принимает объект типа `T`.
> 1. Сначала я проверяю аргумент на `null`. По условию задачи хранить `null` нельзя, поэтому выбрасываю `IllegalArgumentException`.
> 2. Затем проверяю, не занята ли коробка. Если поле `item` не `null`, значит коробка полна, и я выбрасываю `IllegalStateException`.
> 3. Если все проверки пройдены, я сохраняю объект в поле `item`."

🎓 **Теория:**
*   **Инварианты класса:** Условия, которые всегда должны быть истинными для корректного состояния объекта (здесь: в коробке не может быть null, и нельзя класть в полную коробку).

### 1.3 Метод take (Взять)
```java
public T take() {
    T result = this.item;
    this.item = null;
    return result;
}
```
🗣 **Что говорить:**
> "Метод `take` извлекает объект. Я сохраняю текущее значение во временную переменную `result`, затем обнуляю поле `item` (делаю коробку пустой) и возвращаю сохраненное значение. Если коробка была пуста, вернется `null` (или можно добавить проверку и исключение, если требуется строже)."

### 1.4 Методы isFull и peek
```java
public boolean isFull() {
    return this.item != null;
}

public T peek() {
    return this.item;
}
```
🗣 **Что говорить:**
> "`isFull` проверяет наличие предмета. `peek` позволяет посмотреть содержимое без извлечения (не обнуляя ссылку)."

---

## Блок 2: Интерфейс Comparable
Файл: `ru.nikzan.generics.Comparable.java`

```java
public interface Comparable<T> {
    int compare(T other);
}
```
🗣 **Что говорить:**
> "Это обобщенный интерфейс для сравнения объектов. Он содержит один метод `compare`, который принимает объект типа `T` и возвращает целое число: отрицательное, если `this < other`, ноль, если равны, и положительное, если `this > other`."

🎓 **Теория:**
*   **Зачем свой интерфейс?** В Java есть стандартный `java.lang.Comparable`, но в учебных целях мы создали свой, чтобы понять принцип его работы.

---

## Блок 3: Поиск максимума (BoxUtils)
Файл: `ru.nikzan.generics.BoxUtils.java`

### Метод findMax
```java
public static double findMax(Box<? extends Number>... boxes) {
    // ... проверки на null и пустоту ...
    Double max = null;
    for (Box<? extends Number> box : boxes) {
        if (box.isFull()) {
            double value = box.peek().doubleValue();
            if (max == null || value > max) {
                max = value;
            }
        }
    }
    // ... возврат результата ...
}
```
🗣 **Что говорить:**
> "Метод `findMax` ищет максимальное число среди содержимого коробок.
> **Важно:** В сигнатуре используется `Box<? extends Number>`. Это позволяет методу принимать коробки с любыми числовыми типами: `Box<Integer>`, `Box<Double>`, `Box<Float>`.
> Внутри цикла я проверяю, полна ли коробка. Если да, я получаю значение через `peek()`, привожу его к `double` (так как это самый вместительный примитивный тип) и сравниваю с текущим максимумом."

🎓 **Теория:**
*   **Upper Bounded Wildcard (`? extends Number`):** Означает "любой тип, который является `Number` или его наследником". Это делает метод более гибким (принцип PECS - Producer Extends). Мы только *читаем* из коробок (они "производители" данных), поэтому используем `extends`.
*   **Varargs (`...`):** Позволяет передавать произвольное количество аргументов. Внутри метода `boxes` воспринимается как массив.

---

## Блок 4: Функциональные утилиты (ListUtils)
Файл: `ru.nikzan.functional.ListUtils.java`

### 4.1 Метод map (Преобразование)
```java
public static <T, P> List<P> map(List<T> list, Function<T, P> function) { ... }
```
🗣 **Что говорить:**
> "Метод `map` преобразует список элементов типа `T` в список элементов типа `P`.
> Мы используем интерфейс `Function<T, P>`, у которого есть метод `apply`. Мы проходим по исходному списку, применяем функцию к каждому элементу и складываем результаты в новый список."

🎓 **Теория:**
*   **Function<T, R>:** Функциональный интерфейс. Метод: `R apply(T t)`.

### 4.2 Метод filter (Фильтрация)
```java
public static <T> List<T> filter(List<T> list, Predicate<T> predicate) { ... }
```
🗣 **Что говорить:**
> "Метод `filter` отбирает элементы, удовлетворяющие условию.
> Используется интерфейс `Predicate<T>` с методом `test`. Если `test` возвращает `true`, элемент добавляется в результирующий список."

🎓 **Теория:**
*   **Predicate<T>:** Функциональный интерфейс. Метод: `boolean test(T t)`.

### 4.3 Метод reduce (Свертка/Агрегация)
```java
public static <T> T reduce(List<T> list, T identity, BinaryOperator<T> operator) { ... }
```
🗣 **Что говорить:**
> "Метод `reduce` сводит весь список к одному значению.
> `identity` — начальное значение аккумулятора.
> `operator` — это `BinaryOperator`, который берет два значения (накопленное и текущее) и возвращает одно (новое накопленное).
> Пример: сумма чисел списка, где `identity = 0`, а оператор — сложение."

🎓 **Теория:**
*   **BinaryOperator<T>:** Функциональный интерфейс. Метод: `T apply(T t1, T t2)`.

### 4.4 Метод collect (Сборка)
```java
public static <T, P> P collect(List<T> list, Supplier<P> supplier, BiConsumer<P, T> accumulator) { ... }
```
🗣 **Что говорить:**
> "Метод `collect` позволяет собрать элементы списка в любую другую структуру данных (например, в другую коллекцию или строку).
> `Supplier<P>` создает пустой контейнер результата (например, `new ArrayList<>`).
> `BiConsumer<P, T>` определяет, как добавлять элемент `T` в контейнер `P` (например, `List::add`)."

🎓 **Теория:**
*   **Supplier<T>:** Поставщик. Метод: `T get()`. Не принимает аргументов, возвращает объект.
*   **BiConsumer<T, U>:** Потребитель двух аргументов. Метод: `void accept(T t, U u)`. Выполняет действие, ничего не возвращает.

---

## Заключение
В ходе работы были реализованы основные паттерны обобщенного программирования и функциональной обработки данных, что позволяет писать гибкий, переиспользуемый и типобезопасный код.
