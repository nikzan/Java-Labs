# План защиты лабораторной работы №4 (Generics & Functional Interfaces)

Этот документ поможет подготовиться к защите лабораторной работы. Он разбит по задачам и содержит два типа информации:
1.  🗣 **Что говорить** — примерный текст вашего ответа преподавателю при демонстрации кода.
2.  🎓 **Теория (Сноски)** — объяснение терминов и концепций, если преподаватель задаст дополнительный вопрос "А почему именно так?".

---

## Введение
**Цель работы:** Изучение обобщенного программирования (Generics) для создания универсальных типов данных и использование функциональных интерфейсов для обработки коллекций.

---

## Блок 1: Обобщенная Коробка (Box)

### Код: Объявление класса
```java
public class Box<T> {
    private T item;
    // ...
}
```

🗣 **Что говорить:**
> "Я создал класс `Box`, параметризованный типом `T`. Это позволяет использовать одну и ту же реализацию коробки для хранения объектов любых типов (Integer, String и т.д.), сохраняя строгую типизацию. Поле `item` имеет тип `T`."

🎓 **Теория:**
*   **Что такое `T`?** Это *type parameter* (параметр типа). При создании объекта `new Box<Integer>()` вместо `T` подставляется `Integer`.
*   **Зачем это нужно?** Чтобы избежать постоянного приведения типов (casting) и ошибок `ClassCastException` в рантайме, которые были бы неизбежны при использовании простого `Object`.

### Код: Метод put
```java
public void put(T item) {
    if (this.item != null) {
        throw new IllegalStateException("Коробка полна");
    }
    if (item == null) {
        throw new IllegalArgumentException("Нельзя положить null"); // Важное требование!
    }
    this.item = item;
}
```

🗣 **Что говорить:**
> "Метод `put` принимает аргумент типа `T`. Перед вставкой я проверяю два условия:
> 1. Не занята ли коробка (иначе `IllegalStateException`).
> 2. Не является ли переданный объект `null` (иначе `IllegalArgumentException`), так как по условию хранить null нельзя."

---

## Блок 2: Поиск максимума и Wildcards (BoxUtils)

### Код: Метод max
```java
public static double max(Box<? extends Number>... boxes) {
    double max = 0.0;
    boolean first = true;
    for (Box<? extends Number> box : boxes) {
        if (!box.isEmpty()) {
            Number n = box.peek();
            if (first) {
                max = n.doubleValue();
                first = false;
            } else if (n.doubleValue() > max) {
                max = n.doubleValue();
            }
        }
    }
    return max;
}
```

🗣 **Что говорить:**
> "В классе `BoxUtils` реализован статический метод `max`. Он принимает переменное количество коробок (varargs).
> **Ключевой момент:** В сигнатуре я использую `Box<? extends Number>`. Это позволяет мне передавать в метод не только `Box<Number>`, но и `Box<Integer>`, `Box<Double>` и любые другие коробки, где тип содержимого наследуется от `Number`.
> Внутри метода я извлекаю значения, привожу их к `double` (так как это самый широкий примитивный тип для чисел) и нахожу максимум."

🎓 **Теория:**
*   **Что такое `? extends Number`?** Это *Upper Bounded Wildcard* (джокер с верхней границей). Он означает "Любой тип, который является `Number` или его наследником".
*   **Почему нельзя написать просто `Box<Number>`?** Потому что дженерики в Java **инвариантны**. `Box<Integer>` **НЕ** является подтипом `Box<Number>`. Если бы мы написали `Box<Number>`, мы не смогли бы передать туда коробку с целыми числами.
*   **Почему `doubleValue()`?** Класс `Number` имеет абстрактный метод `doubleValue()`, который реализован во всех числовых обертках. Это универсальный способ получить числовое значение.

---

## Блок 3: Функциональные интерфейсы (ListUtils)

### Код: Метод map
```java
public static <T, R> List<R> map(List<T> list, Function<T, R> mapper) {
    List<R> result = new ArrayList<>();
    for (T item : list) {
        result.add(mapper.apply(item));
    }
    return result;
}
```

🗣 **Что говорить:**
> "Метод `map` преобразует список одного типа `T` в список другого типа `R`.
> Он принимает исходный список и функциональный интерфейс `Function<T, R>`.
> Мы проходим по списку, применяем метод `apply` интерфейса к каждому элементу и складываем результат в новый список."

🎓 **Теория:**
*   **Что такое `Function<T, R>`?** Это стандартный функциональный интерфейс из `java.util.function`. Он принимает аргумент типа `T` и возвращает результат типа `R`.
*   **Пример использования:** `s -> s.length()` (преобразование String в Integer).

### Код: Метод filter
```java
public static <T> List<T> filter(List<T> list, Predicate<T> predicate) {
    List<T> result = new ArrayList<>();
    for (T item : list) {
        if (predicate.test(item)) {
            result.add(item);
        }
    }
    return result;
}
```

🗣 **Что говорить:**
> "Метод `filter` фильтрует список. Он принимает `Predicate<T>`.
> Если метод `test` предиката возвращает `true`, элемент добавляется в результирующий список."

🎓 **Теория:**
*   **Что такое `Predicate<T>`?** Функциональный интерфейс, принимающий `T` и возвращающий `boolean`. Используется для проверки условий.

### Код: Метод reduce
```java
public static <T> T reduce(List<T> list, T identity, BinaryOperator<T> accumulator) {
    T result = identity;
    for (T item : list) {
        result = accumulator.apply(result, item);
    }
    return result;
}
```

🗣 **Что говорить:**
> "Метод `reduce` сводит список к одному значению (агрегация).
> `identity` — это начальное значение.
> `accumulator` — это `BinaryOperator`, который берет текущий накопленный результат и следующий элемент списка, и объединяет их."

🎓 **Теория:**
*   **Что такое `BinaryOperator<T>`?** Это специализация `BiFunction<T, T, T>`. Принимает два объекта типа `T` и возвращает один объект типа `T`.
*   **Пример:** Сложение чисел, конкатенация строк.

---

## Общие вопросы, которые могут задать

**В: Что такое стирание типов (Type Erasure)?**
**О:** Дженерики существуют только на этапе компиляции. Компилятор проверяет типы, а затем "стирает" их, заменяя `T` на `Object` (или на верхнюю границу, например `Number`). В байт-коде `Box<Integer>` и `Box<String>` превращаются в один и тот же класс `Box`.

**В: В чем разница между `List<Object>` и `List<?>`?**
**О:**
*   `List<Object>` — список, в который можно класть *что угодно*, но сам список должен быть именно `List<Object>` (нельзя присвоить ему `List<String>`).
*   `List<?>` — список *неизвестного типа*. Ему можно присвоить `List<String>`, но в него *ничего нельзя положить* (кроме `null`), так как компилятор не знает, какой там тип данных.

**В: Что такое лямбда-выражение?**
**О:** Это краткая запись анонимного класса, реализующего функциональный интерфейс (интерфейс с одним абстрактным методом). Например: `x -> x * 2`.
